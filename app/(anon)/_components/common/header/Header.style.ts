export const headerStyles = {
  wrapper:
    'sticky top-0 z-50 w-full h-16 flex items-center justify-between border-b bg-white px-[max(var(--page-x),env(safe-area-inset-left))]',
  backButton: 'text-base',
};
